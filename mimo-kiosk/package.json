{
  "name": "mimo-kiosk-wrapper",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "npm --prefix mimo-frontend install && npm --prefix mimo-frontend run build",
    "dev": "npm --prefix mimo-frontend run dev"
  }
}